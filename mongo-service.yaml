apiVersion: v1
kind: Service
metadata:
  annotations:
    cloud.google.com/neg: '{"ingress":true}'
  finalizers:
  - service.kubernetes.io/load-balancer-cleanup
  labels:
    app: mongo
  name: mongo
  namespace: default
  resourceVersion: "3956"
spec:
  ports:
    port: 27017
    targetPort: 27017
  selector:
    app: mongo
